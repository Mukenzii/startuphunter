# Public origin of the Django backend API (no trailing slash).
# In local dev this defaults to http://localhost:8000 if unset.
VITE_API_BASE_URL=https://your-backend-host.example

# Google OAuth web client ID used for "Sign up with Google".
VITE_GOOGLE_CLIENT_ID=
